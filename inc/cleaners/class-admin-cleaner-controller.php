<?php
/**
 * Admin cleanup and imporve user flow
 *
 * @since 1.0.0
 */

/* Die if accessed directly */
if( ! defined( "ABSPATH" ) ) {
	die("Direct access is not permitted");
}

if( ! class_exists('Raven_admin_cleaner_controller') ) {
	class Raven_admin_cleaner_controller {
		
		/**
		 * Constructor
		 *
		 * @since 1.0.0
		 */
		function __construct() {}
	
	}	
}